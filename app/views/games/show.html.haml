%h1= @game.name
#current-round
  = render partial: 'rounds/current', locals: { round: @round }

%h2 Your Cards

#hands
  = render partial: 'hands/current'

= form_tag '' do
  = hidden_field_tag 'game_id', @game.id
  = hidden_field_tag 'current_round_id', @game.current_round.id
